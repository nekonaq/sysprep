# -*- sh -*-

conf_hook() {
  local conf="$1"
  export ID= VERSION_ID= VERSION_CODENAME=
  . /etc/os-release
  if [ "${VERSION_ID%%.*}" -le 20 ]; then
    echo "$conf"
    return
  fi
  echo "$here/install@emacs.yml"
}
